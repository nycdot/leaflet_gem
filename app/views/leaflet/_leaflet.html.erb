<% content_for :scripts do %>

	<script type="text/javascript" charset="utf-8">

		<%=raw generate_map(options) %>

	</script>
<% end %>
<div id="<%= options[:mapid] %>" class="<%= options[:class] unless options[:class].nil? %>" style="<%= options[:style] unless options[:style].nil? %>"></div>
